# add only vmware nodes, pattern:
# Since vmware nodes only have one interface setup,
# you need to manually add all IPs to the node
# HOSTNAME		   User		Variable
[vmware-nodes]

[loadbalancer-nodes]
# add all lb nodes, pattern:
# HOSTNAME          User            Hostname (optional)
# lb1.example.com ansible_user=root hostname_overwrite=
lb-0.staging.example.org ansible_user=root

[kubernetes-nodes]
# add all nodes (excluding lb), pattern:
# HOSTNAME          User              Variable
nd-0.staging.example.org ansible_user=root
nd-1.staging.example.org ansible_user=root

[kubernetes-masters]
# add all masters, pattern :
# mst1.example.com ansible_user=root
# Please use FQDNs for the hosts, IP addresses are not supported anymore
mstr-0.staging.example.org ansible_user=root

[kubernetes-management]
localhost ansible_user=myuser
